import { makeStyles } from "@material-ui/core/styles";
//exporting the styles function for @material-ui to be used in other components.
export default makeStyles(() => {
  ul: {
    justifyContent: "space-around";
  }
});